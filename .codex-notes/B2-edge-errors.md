| Severity | File:line | Scenario | Fix |
|---|---|---|---|
| HIGH | `apps/backend/src/ws/socket.ts:48` | Very large/rapid terminal output is sent with `ws.send()` without checking `bufferedAmount`, send callbacks, or per-peer limits. Slow clients can build unbounded server memory. | Add bounded per-peer output queues/backpressure; drop/coalesce output for slow peers and rely on snapshot; terminate peers above a buffer threshold. |
| HIGH | `apps/frontend/app/stores/terminals.ts:83`, `apps/backend/src/ws/socket.ts:68` | Terminal exits/crashes or backend restarts: WS status omits `pid` when stopped/exited, and client only updates `pid` if defined, leaving stale PIDs visible. | Send full runtime status or `pid: null`; client should clear `pid` on non-running states. |
| MEDIUM | `apps/frontend/app/components/TerminalPane.vue:158`, `apps/frontend/app/components/TerminalSidebar.vue:55` | Start/stop/restart API failures are unhandled in click handlers. Invalid cwd/custom shell can become an unhandled rejection with no user-facing error. | Wrap lifecycle calls in store/component error handling, show toast, and reconcile with `fetchAll()` on 404/connection failure. |
| MEDIUM | `apps/backend/src/ws/socket.ts:68`, `apps/frontend/app/stores/terminals.ts:83` | Failed starts lose the actual error. Backend stores `status.error`, but WS broadcasts only `{ state, pid }`, so UI shows `failed` without why. | Include `error`, `exitCode`, `signal`, `cols`, `rows` in `term.status`; render failure reason in pane/list. |
| MEDIUM | `apps/backend/src/http/terminals.routes.ts:77`, `apps/backend/src/http/terminals.routes.ts:108` | Bad/relative `cwd` is saved successfully and only fails later on start, producing confusing delayed failure. | Validate cwd on create/update using the same absolute-local-path rules as `/api/fs`; return 400 before persisting. |
| MEDIUM | `apps/backend/src/http/terminals.routes.ts:79`, `apps/backend/src/http/terminals.routes.ts:113` | Invalid shell payload silently falls back to default on create or is ignored on update. Direct/API users think their value was accepted. | Treat invalid provided shell as 400; only use default when shell is absent. |
| MEDIUM | `apps/backend/src/terminal/TerminalManager.ts:184`, `apps/backend/src/terminal/TerminalSession.ts:142` | Broadcast send can report `written++` even if the terminal exited between target resolution/start and `write()`. Command is silently dropped. | Make `write()` return boolean/result; count success only when a live pty accepted data, otherwise add `EXITED`/`NOT_RUNNING`. |
| MEDIUM | `apps/frontend/app/components/TerminalListItem.vue:22`, `apps/frontend/app/components/TerminalPane.vue:158` | Rapid repeated clicks can queue duplicate restarts/deletes and produce 404s/unhandled rejections. Backend serializes sessions, but UI has no per-terminal pending guard. | Track per-terminal pending operations; disable or collapse duplicate start/stop/restart/delete; catch 404 and refresh state. |
| MEDIUM | `apps/backend/src/http/terminals.routes.ts:126`, `apps/frontend/app/stores/terminals.ts:60` | Deleting active/selected terminal is reconciled only in the deleting client. Other open clients keep stale active/selected terminals until a later fetch/error. | Broadcast `term.deleted` or force `fetchAll()` on delete/status unknown; remove id from active and selections in all clients. |
| LOW | `apps/backend/src/terminal/TerminalManager.ts:150`, `apps/frontend/app/composables/useSocket.ts:114` | Empty selections, missing groups, or zero terminals can return success: “Sent to 0 terminal(s)”. | Reject `matched === 0` as `NO_TARGETS`; validate group existence server-side and show a clear UI error. |
| LOW | `apps/frontend/app/components/CommandBar.vue:20` | Empty command input can be sent, usually as just Enter because backend appends newline. | Disable send for empty input, or expose a deliberate “send Enter” action. |
| LOW | `apps/backend/src/ws/protocol.ts:120`, `apps/backend/src/ws/socket.ts:82` | Malformed JSON, binary frames, invalid resize/input, and attach-limit overflow are silently ignored. | Return `term.error` with `BAD_REQUEST`/`TOO_MANY_ATTACHMENTS`, or close protocol-invalid sockets with an explicit close code. |
| LOW | `apps/frontend/app/components/TerminalEditModal.vue:71`, `apps/backend/src/terminal/TerminalSession.ts:205` | Special characters are not preserved consistently: shell args are split on whitespace; initial commands are trimmed before execution. | Store shell args as an array editor or parse quoted args; do not trim command execution text except for empty-checking. |

Static review only; no tests were run.