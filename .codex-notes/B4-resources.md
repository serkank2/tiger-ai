**Findings**

| Severity | File:line | Issue | Fix |
|---|---|---|---|
| HIGH | [apps/backend/src/ws/socket.ts](/C:/Users/serkan/Desktop/Kaplan/apps/backend/src/ws/socket.ts:48) | `ws.send()` is used for terminal output without checking `bufferedAmount` or applying backpressure. A slow attached peer can accumulate unbounded outbound buffers during high-output sessions. | Add a per-peer send-buffer limit. For `term.output`, drop/coalesce chunks or detach/terminate peers over the threshold; keep status/error messages small and prioritized. |
| HIGH | [apps/backend/src/terminal/TerminalSession.ts](/C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalSession.ts:216) | Stop/delete can mark a session `stopped` even if the pty never exits after force kill. Because `proc.onData()` / `proc.onExit()` disposables are discarded at lines 198-201, a surviving pty can keep callbacks, the `TerminalSession`, and possibly child processes alive after `remove()`. | Store node-pty listener disposables, dispose them in `handleExit()`/`dispose()`, invalidate `generation` before giving up, and do not report `stopped` unless the process exited. On hard-kill timeout, surface failure or keep a `killing/failed` state. |
| MEDIUM | [apps/backend/src/terminal/TerminalManager.ts](/C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalManager.ts:76) | Stopped/exited sessions stay in `sessions` indefinitely, retaining up to `scrollbackBytes` each. The ring is capped per session, but aggregate memory grows with every terminal ever started until deletion/backend restart. | Add global scrollback/session budget, TTL/LRU eviction for stopped sessions, or clear buffers on stop when snapshots are no longer needed. |
| MEDIUM | [apps/frontend/app/app.vue](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/app.vue:31) | Preview polling starts after `await loadAll()`. If the component unmounts before load completes, the interval can be created after cleanup. The `setInterval` also allows overlapping `refreshPreviews()` requests if one run takes longer than 4s. | Use a disposed flag plus recursive `setTimeout` scheduled after each request finishes; guard with `inFlight`, and abort/cancel on unmount. |
| LOW | [apps/backend/src/index.ts](/C:/Users/serkan/Desktop/Kaplan/apps/backend/src/index.ts:50) | The returned `WebSocketServer` is discarded, so shutdown only calls `server.close()`. Existing WS clients, heartbeat cleanup, and manager listener cleanup rely on process exit rather than explicit `wss.close()`. | Keep `const wss = createWsServer(...)`; during shutdown terminate clients, call/await `wss.close()`, then close the HTTP server. |
| LOW | [apps/backend/src/ws/socket.ts](/C:/Users/serkan/Desktop/Kaplan/apps/backend/src/ws/socket.ts:100) | `peer.attached` is capped, but stale deleted terminal IDs are not pruned server-side. Cross-client/delete races can fill the set with dead IDs and block future attaches at `MAX_ATTACH`. | On terminal delete, broadcast/prune that ID from all peers; also delete unknown IDs from `peer.attached` when attach validation fails. |
| LOW | [apps/frontend/app/components/TerminalPane.vue](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/components/TerminalPane.vue:80) | Normal terminal switches clean up correctly, but mount has no `try/catch`. If xterm construction/addon load/open or a later socket send throws, partially-created xterm/listeners may survive until the next teardown. | Wrap mount setup in `try/catch`; dispose local `Terminal`, listener off-functions, and observer on failure before rethrow/logging. |
| LOW | [apps/frontend/app/composables/useSocket.ts](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:62) | Reconnect timer/socket cleanup exists for HMR only. A root unmount while disconnected can leave a reconnect timer that reconnects without UI ownership. | Add a public `disconnect()` that clears reconnect timer, closes socket, and optionally clears attached/listener maps; call it from app teardown. |
| LOW | [apps/frontend/app/stores/notices.ts](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/stores/notices.ts:11) | Each notice creates an independent timeout and `items` has no cap. Under repeated WS errors/broadcast results, concurrent notices/timers can grow with event rate. | Cap active notices, coalesce duplicate text, and store timeout IDs so they can be cleared on dismiss/store disposal. |

**Checked OK**

- `TerminalPane` normal switch/unmount path detaches the socket, removes snapshot/output subscriptions, disposes xterm `onData`/`onResize`, disconnects `ResizeObserver`, and disposes the xterm instance at [TerminalPane.vue:49](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/components/TerminalPane.vue:49).
- Snapshot/output listener maps delete empty sets on unsubscribe at [useSocket.ts:146](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:146).
- Delete-confirm timer is cleared on component unmount at [TerminalListItem.vue:33](/C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/components/TerminalListItem.vue:33); no material leak there.
- No CRITICAL findings.