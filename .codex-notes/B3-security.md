## Security Review

Findings first, ordered by severity.

| Severity | File:line | Issue | Fix |
|---|---|---|---|
| HIGH | [terminals.routes.ts:77](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:77), [terminals.routes.ts:108](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:108), [settings.routes.ts:19](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/settings.routes.ts:19), [TerminalSession.ts:179](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalSession.ts:179), [TerminalSession.ts:313](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalSession.ts:313) | Terminal `cwd` and `settings.defaultCwd` bypass the fs route’s `safePath()` checks. They accept relative paths, root-relative Windows paths, UNC/device paths, and `..`-based paths, then `fs.stat()`/`pty.spawn()` touch them. On Windows, UNC paths can trigger outbound SMB access/credential exposure. | Move `safePath()` into shared validation, make it require fully-qualified local absolute directories, reject UNC/device/root-relative/relative paths before persistence and again before spawn, and use the resolved path for `pty.spawn()`. |
| MEDIUM | [index.ts:25](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/index.ts:25), [terminals.routes.ts:142](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:142), [terminals.routes.ts:152](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:152), [terminals.routes.ts:161](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:161) | CORS is configured, but HTTP routes do not enforce Origin server-side. CORS blocks disallowed browser reads and JSON preflighted writes, but simple cross-origin POSTs can still hit `start`/`stop`/`restart` if a terminal id is known. | Add an HTTP origin guard before routes: if `Origin` is present and not allowlisted, return 403. Also require `Content-Type: application/json` for mutating JSON routes and reject body-bearing simple POSTs. |
| MEDIUM | [validate.ts:20](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/validate.ts:20), [validate.ts:21](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/validate.ts:21), [TerminalSession.ts:180](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalSession.ts:180), [TerminalSession.ts:183](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/terminal/TerminalSession.ts:183) | `custom` shell `path` and shell `args` are accepted as arbitrary strings. This is not shell-metacharacter injection because `node-pty.spawn(file, args, ...)` uses argv, but a UNC/device custom shell path can still cause remote access or run an unintended executable. | Validate custom shell paths as fully-qualified local executable paths, reject UNC/device paths, and consider max lengths/counts for `args`. Keep using argv spawning. |
| LOW | [fs.routes.ts:15](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/fs.routes.ts:15), [fs.routes.ts:16](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/fs.routes.ts:16), [fs.routes.ts:17](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/fs.routes.ts:17) | fs routes reject UNC, but Windows root-relative paths like `\Windows` or `/Windows` can pass `path.isAbsolute()` and resolve against the backend process drive. That is ambiguous for an “absolute local path” validator. | On Windows, require drive-qualified paths such as `C:\...` after normalization, and reject single-root paths. |
| LOW | [groups.routes.ts:24](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/groups.routes.ts:24), [groups.routes.ts:40](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/groups.routes.ts:40) | Group `color` accepts arbitrary strings and is later used as a CSS background value. Vue style binding avoids HTML injection, but arbitrary CSS values can still cause rendering abuse or external URL fetches. | Whitelist the known palette or validate strict hex colors, with a short max length. |
| LOW | [index.ts:26](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/index.ts:26) | JSON bodies are capped at `1mb`, which is good, but the cap only applies to JSON-parsed requests. Non-JSON simple requests can still send large bodies to routes that do not need a body. | Reject unsupported content types on mutating routes and add request timeout/body-drain limits or a small global raw-body guard. |
| LOW | [terminals.routes.ts:78](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:78), [terminals.routes.ts:80](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/terminals.routes.ts:80), [groups.routes.ts:16](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/groups.routes.ts:16), [settings.routes.ts:29](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/http/settings.routes.ts:29) | Field-level limits are mostly absent. Within the 1MB JSON limit, a local client can persist very large names, commands, env maps, shell args, groups, or settings and bloat state/UI. | Add schema validation with max string lengths, max env keys, max shell args, and practical max terminal/group counts. |

## Checks With No Finding

| Area | Result |
|---|---|
| WebSocket Origin | Good: [socket.ts:39](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/ws/socket.ts:39) rejects browser WebSockets whose `Origin` is not in `config.corsOrigins`; no-Origin local non-browser clients are allowed by design. |
| WebSocket body limits | Good: [socket.ts:35](C:/Users/serkan/Desktop/Kaplan/apps/backend/src/ws/socket.ts:35) sets `maxPayload` to 512 KiB. |
| Command injection via args/cwd | No direct shell interpolation found. `cwd` is passed as a spawn option, and shell args are passed as argv. `initialCommand` is intentionally written into the shell and should be treated as trusted terminal configuration. |
| Cross-origin simple GET | GET routes can be triggered cross-origin, but disallowed browser pages cannot read responses without CORS. I did not see GET side effects. The fs GETs still perform local `stat/readdir`, so the server-side Origin guard above is useful hardening. |