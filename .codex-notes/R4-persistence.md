No CRITICAL findings. Main persistence risks:

| Severity | Location | What is wrong | Concrete fix |
|---|---|---|---|
| HIGH | `apps/backend/src/store/state.ts:77` | If `state.json` is corrupt but `state.json.bak` is valid, the loader returns the backup but leaves the corrupt primary in place. The next save then copies the corrupt primary over `.bak` at `state.ts:119` before replacing the primary. A crash or rename failure at that point can lose the last good backup. | When backup recovery succeeds, immediately rename the corrupt primary to `state.corrupt.<ts>.json`, promote the recovered backup by saving it, and only then continue startup. Also avoid overwriting `.bak` from an unvalidated primary. |
| HIGH | `apps/backend/src/store/state.ts:109` | The write is not fully crash-durable. The tmp file is fsynced, but the directory is never fsynced after creating/renaming files, and `.bak` is created with `copyFile` without fsyncing the copied backup. After power loss, the rename or backup update may not be durable. | Write `state.json.tmp`, `fh.sync()`, close, then fsync the parent directory after final `rename`. For `.bak`, write/copy to a backup temp, fsync that file, rename it to `.bak`, and fsync the directory after each rename. Use best-effort directory fsync on platforms that support it. |
| MEDIUM | `apps/backend/src/store/state.ts:70` | If `state.json` is missing, the loader immediately creates a fresh default state and ignores an existing `state.json.bak`. That can discard recoverable data after manual deletion, directory-entry loss, or a failed recovery path. | On `ENOENT`, try loading and promoting `state.json.bak` before creating defaults. Only write a fresh state when neither primary nor backup exists. |
| MEDIUM | `apps/backend/src/store/state.ts:45` | `normalize()` trusts malformed persisted data. Invalid `settings.theme`, non-string `defaultCwd`, invalid `defaultShell`, bad command-routing booleans, and malformed terminal/group entries pass through because of shallow spreads and array checks. This can persist bad state and later break terminal startup. | Add runtime validators for `AppSettings`, `ShellSpec`, `TerminalDefinition`, and `TerminalGroup`. Reuse the request validators where possible, drop or repair invalid entries, and only return a fully valid `PersistedState`. |
| MEDIUM | `apps/backend/src/store/state.ts:47` | Unknown or future schema versions are silently coerced to version 1. If a newer app writes fields this version does not understand, this loader will normalize and later save over them, causing downgrade data loss. | Check `schemaVersion`. Migrate known older versions explicitly. For unknown newer versions, refuse to load or preserve the file and require migration instead of silently rewriting it as v1. |
| MEDIUM | `apps/backend/src/store/state.ts:97` | The write chain serializes operations, but it queues the mutable `state` object by reference. Concurrent `saveState(state)` calls can write whatever the shared object contains when the queued write actually runs, not the snapshot at call time. | Snapshot at enqueue time, e.g. `const snapshot = structuredClone(state)` before appending to `writeChain`, then write that snapshot. If intentional last-write-wins coalescing is desired, make that explicit and ensure failed request mutations cannot be persisted by later saves. |
| LOW | `apps/backend/src/store/state.ts:69` | All read/parse errors are treated as corruption. Non-corruption filesystem errors like permission problems, transient I/O errors, or wrong file type can enter the backup/reset path. | Only recover/reset for JSON parse or validation failures. For operational filesystem errors, surface the error and avoid renaming or overwriting state. |