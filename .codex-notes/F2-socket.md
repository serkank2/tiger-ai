**Findings**

| SEVERITY | file:line | issue | concrete fix |
|---|---|---|---|
| HIGH | [apps/frontend/app/composables/useSocket.ts:29](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:29) | Reconnect re-attaches every terminal, and backend attach replays scrollback. Existing xterm contents are not cleared, so every reconnect duplicates the full buffer in the visible terminal. | Distinguish initial attach from reconnect attach. Add protocol support like `term.attach { replay: false }` for reconnects, or send replay as a separate snapshot message that the terminal view replaces/clears before rendering. |
| MEDIUM | [apps/frontend/app/composables/useSocket.ts:3](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:3) | Module-scoped singleton is not HMR-safe. A hot reload resets `socket`, `reconnectTimer`, `outputListeners`, and `attached` in the new module, while the old WebSocket/timer/listeners can keep running. That can create multiple live sockets and stale callbacks. | Add `import.meta.hot.dispose()` to close the socket, clear the timer, and clear listener/attach state; or persist one singleton through `import.meta.hot.data` and rebind handlers deliberately. |
| MEDIUM | [apps/frontend/app/composables/useSocket.ts:77](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:77) | `attached` is a plain `Set`, so it is not safe for multiple component callers. If two callers attach the same terminal, the first `detach(id)` removes the server subscription for all remaining listeners. | Replace with `Map<string, number>` ref counts, or return a per-attach disposer from `attach(id)` and only send `term.detach` when the last owner releases. |
| MEDIUM | [apps/frontend/app/composables/useSocket.ts:56](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:56) | `raw()` silently drops messages unless the socket is already `OPEN`. `attach` survives because IDs are stored, but latest `resize`, `input`, and `broadcast` can be lost during connect/reconnect with no caller feedback. | Track durable state separately: keep latest size per attached terminal and replay `attach` plus `resize` on open. For `input`/`broadcast`, return `false`/throw or disable UI while disconnected instead of silently dropping. |
| MEDIUM | [apps/frontend/app/composables/useSocket.ts:31](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:31) | Close/open handlers are not tied to the specific `WebSocket` instance. A stale closing socket can still set status to disconnected or schedule reconnect after a newer socket exists. | In `connect()`, capture `const ws = new WebSocket(...)`; in every handler, ignore events when `socket !== ws`. Set `socket = null` on current close and clear pending reconnect timers on successful open. |
| LOW | [apps/frontend/app/composables/useSocket.ts:60](C:/Users/serkan/Desktop/Kaplan/apps/frontend/app/composables/useSocket.ts:60) | Dispatcher ignores `term.attached`, `term.error`, and `term.broadcastResult`, and the broad `try/catch` around `handle()` also hides listener exceptions. One bad output listener can stop later listeners for that frame. | Use a typed exhaustive `ServerMessage` union. Handle or intentionally log every server message type. Parse JSON in its own `try`, then dispatch; iterate `Array.from(set)` and isolate/log individual listener failures. |

No serious issue found in `apps/frontend/app/composables/useApi.ts`; it is just a thin `$fetch` wrapper.