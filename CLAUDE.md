# CLAUDE.md

This repository's guidance for AI coding agents lives in [`AGENTS.md`](AGENTS.md).

**Claude Code (and any other agent): read [`AGENTS.md`](AGENTS.md) first.** It is the single source
of truth for the project summary, repository map, prerequisites, the exact build/test/lint commands,
conventions, and the do-not rules. This file is only a pointer — it intentionally duplicates nothing,
so there is one place to keep current.
